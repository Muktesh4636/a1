# Django Settings - SECURITY: Production defaults
DEBUG=False
# Generate secret key: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=CHANGE_THIS_TO_A_VERY_LONG_RANDOM_SECRET_KEY
ALLOWED_HOSTS=gunduata.online,www.gunduata.online,72.61.254.71

# Database Configuration - SECURITY: Use strong passwords
USE_SQLITE=False
DB_NAME=dice_game
DB_USER=postgres
DB_PASSWORD=CHANGE_THIS_TO_A_STRONG_RANDOM_PASSWORD_MIN_16_CHARS
DB_HOST=db
DB_PORT=5432

# Redis Configuration - SECURITY: Use password authentication
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=CHANGE_THIS_TO_A_STRONG_RANDOM_PASSWORD_MIN_16_CHARS
REDIS_POOL_SIZE=100

# SSL/HTTPS Configuration (enable when SSL certificate is installed)
SECURE_SSL_REDIRECT=False
# Set to True after installing SSL certificate

# CORS Configuration - SECURITY: Restrict to your domains only
CORS_ALLOWED_ORIGINS=https://gunduata.online,https://www.gunduata.online

# Firewall Configuration - SECURITY: Multi-layer protection
# Comma-separated list of IPs to permanently block
BLOCKED_IPS=

# Comma-separated list of IPs allowed to access admin panel (leave empty to allow all)
# Example: ADMIN_WHITELIST_IPS=your.office.ip,your.home.ip
ADMIN_WHITELIST_IPS=

# Rate Limiting
RATE_LIMIT_REQUESTS=100  # Max requests per IP per window
RATE_LIMIT_WINDOW=60     # Time window in seconds

# Brute Force Protection
BRUTE_FORCE_THRESHOLD=50  # Failed login attempts before ban (default: 50)
BRUTE_FORCE_BAN_TIME=7200  # Ban duration in seconds (default: 2 hours)

